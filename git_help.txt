1.from home dir, nned to do every time:
       ssh-add ssh.txtt

2. to get the updates:
   git pull - brings the repository
   git rebase -checks the brunch (i think) and say if we are up to date

3. to add your changes:
   git add . - adds everything.
   git add <files> - adds specific files

   git commit -m "note" - commits the changes

   git push - updates commited changes to repo

gitk - shows status, need to make sure yellow dot appers at the top, and the updated branch is there too.
   
